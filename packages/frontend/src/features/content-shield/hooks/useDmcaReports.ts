// Removed: getDmcaReports has no corresponding backend route (GET /dmca/reports).
// The generateDmcaReport method (POST /alerts/:id/dmca-report) remains available
// via useDmcaReport hook for generating individual reports from alerts.

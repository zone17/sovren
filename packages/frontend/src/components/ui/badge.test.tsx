import "@testing-library/jest-dom"; import { render, screen } from "@testing-library/react"; import { Badge } from "./badge"; describe("Badge Component", () => { it("renders correctly", () => { render(<Badge>Test Badge</Badge>); expect(screen.getByText("Test Badge")).toBeInTheDocument(); }); it("default variant styling", () => { render(<Badge>Default</Badge>); const badge = screen.getByText("Default"); expect(badge).toHaveClass("bg-[#0969da]"); }); it("lightning variant styling", () => { render(<Badge variant="lightning">Lightning</Badge>); const badge = screen.getByText("Lightning"); expect(badge).toHaveClass("bg-[#f7931a]"); }); it("sovereign variant styling", () => { render(<Badge variant="sovereign">Sovereign</Badge>); const badge = screen.getByText("Sovereign"); expect(badge).toHaveClass("bg-[#6366f1]"); }); });
